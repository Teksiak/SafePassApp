<h1>Entries</h1>

<div class="row">
  <div class="card">
    <div class="card-body">
      <% if @entries.any? %>
        <%= render(@entries) %>
      <% else %>
        <h6>No entries yet.</h6>
        <%= link_to("Add an Entry", new_entry_path) %>
      <% end %>
    </div>
  </div>
</div>
